/**
 * @author dailey.dai@oracle.com
 * @modify 2014/07/9
 * @file   full calendar configure
 */
function(widget, $) {
	widget.fullcalendar = function(options) {
		var defaults = {
			switchOps : true//shorten first
			,
			fcSel : ''//fullcalendar jquery selector

		};

	};
}(apex.widget, apex.jQuery);
